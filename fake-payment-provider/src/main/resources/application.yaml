spring:
  application:
    name: fake-payment-provider

  datasource:
    url: jdbc:postgresql://localhost:5432/?
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: create

  flyway:
    enabled: true
    locations: classpath:db/migration
server:
  port: 8080


